"""Third-party code vendored into velox.

Vendored, not depended on: velox ships no plugin ecosystem and takes no runtime dependency on
pytest. See `VENDOR.md` for what came from where, and `scripts/vendor_assertion.py` for the
exact edits — re-vendoring is a deliberate act, not a tracked upstream.
"""
