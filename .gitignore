# Python cache files
__pycache__
*.py[cod]
__pypackages__

# Distribution / packaging
.Python
/build
/dist

# Generated by hatch-vcs at build time (pyproject.toml [tool.hatch.build.hooks.vcs])
/velox/_version.py

# Docs site, built by `just docs-build` (zensical.toml [project] site_dir)
/site

# Ruff, uv, pytest, velox
.ruff_cache
.pytest_cache
.velox_cache

# velox-migrate's work directory, written into the suite being migrated
.velox-migrate
