# Application dependencies.
fastapi>=0.115
pydantic[email]>=2.9
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20
httpx>=0.28

# The runner. Unreleased, so install the repo:
#   uv pip install -e ../..
# velox>=0.1
