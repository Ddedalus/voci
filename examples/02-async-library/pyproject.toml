[project]
name = "voci-example-async-library"
version = "0.0.0"
requires-python = ">=3.13"

[tool.voci]
testpaths = ["tests"]
concurrency = 16
timeout = 30

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
